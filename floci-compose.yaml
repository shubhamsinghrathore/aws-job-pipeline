services:
  floci:
    image: floci/floci:latest
    ports:
      - "4566:4566"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock   # needed for ECR / Lambda / container services
    environment:
      - FLOCI_STORAGE_MODE=hybrid                     # state survives restarts
